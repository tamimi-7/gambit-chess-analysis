# Claude Code local dev-server launch config — not part of the project.
.claude/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Stockfish WASM binaries — regenerated from node_modules by `npm run engine`
# (runs automatically before dev/build). ~14 MB, no reason to version them.
public/engine/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
